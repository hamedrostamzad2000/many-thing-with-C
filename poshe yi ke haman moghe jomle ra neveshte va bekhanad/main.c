#include <stdio.h>
#include <stdlib.h>
char b[50];
int c;

int main()
{
    //FILE *fp;
    //fp=fopen("index.dat","w");



    gets(b);
    //scanf("%d",c);

    puts(b);
    /*fputs(b,fp);
    //fprintf(fp,"%d",c);

    //fclose(fp);


    //FILE *fp;
    fp=fopen("index.dat","r");
    fgets(b,59,fp);
    puts(b);

    //fscanf(fp,"%d",&c);
    //printf("%d",c);
    fclose(fp);*/

    return 0;
}
